import type { Metadata } from "next";
import { Geist } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  // CUSTOMIZE: update with your name, title, and description
  title: "Alexander Leung — Creator & Brand Strategist",
  description:
    "Brand portfolio & media kit for Alexander Leung. Live audience metrics, case studies, and partnership information.",
};

export default function RootLayout({
  children,
}: Readonly<{ children: React.ReactNode }>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} antialiased`}>{children}</body>
    </html>
  );
}
